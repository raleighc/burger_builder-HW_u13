<div class="container">
  <div class="row justify-content-center">
    <div class="col-8">
<h1>GROSS Burgers!</h1>

<img src="/assets/images/scale.png" alt="Gross Burger">


<h2>Burgers that are ready to be eaten!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burger-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burger-block devoured=false}}
  {{/if}}
  {{/each}}
</ul>
<br>
<br>
<h3>Add a Disgusting Burger!</h3>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">What's the Burger's Status?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Ready to eat!<br>
    <input type="radio" name="sleepy" value="0"> Devoured
  </div>

  <button class="btn btn-outline-dark" type="submit">Add Burger</button>
</form>

</div>
</div>
</div>